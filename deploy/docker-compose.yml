version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    environment:
      - USE_SECRET_MANAGER=false
    restart: unless-stopped
